name: igenwebapp
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  video_player: ^2.8.1 

  cupertino_icons: ^1.0.8
  intl: ^0.20.2  # Agregamos o actualizamos la dependencia de intl

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # Esta sección es necesaria para que Flutter pueda
  # encontrar y cargar los archivos de imagen.
  assets:
    - assets/images/
    - assets/videos/